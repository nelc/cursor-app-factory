# Environment Configuration for Deployment
# Copy this file to .env and fill in your values

# ============================================
# GCP Configuration (Required for Deployment)
# ============================================
GCP_PROJECT_ID=app-sandbox-factory    # Your GCP project ID
GCP_REGION=us-central1                # GCP region (us-central1, europe-west1, etc.)
SERVICE_NAME=my-app                   # Your application name (lowercase, no spaces)

# ============================================
# Local Development (Optional - docker-compose handles these)
# ============================================
NODE_ENV=development
PORT=8080

# Note: Database credentials are in docker-compose.yml for local dev
# Production uses Kubernetes secrets automatically

